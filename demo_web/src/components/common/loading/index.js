import React from 'react'
import { Loading } from 'tinper-bee'

function loading(){
    return (
        <Loading
            container={this}
            show={true}
        />
    )
}

export default loading